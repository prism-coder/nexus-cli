# <%= projectName %>

This project was generated using [Nexus CLI](https://github.com/prism-coder/nexus-cli) version <%= currentVersion %>.

## Code scaffolding

Nexus CLI includes powerful code scaffolding tools. To generate a new layer, run:

```bash
# nexus create:layer|cl [options] <layer-name>
nexus create:layer Test
```

For a complete list of available schematics (such as `layers`, `events`, or `services`), run:

```bash
nexus --help
```

## Building

To build the project run:

```bash
npm run build
```

This will compile your project and store the build artifacts in the `dist/` directory.

## Additional Resources

For more information on using the Nexus CLI, including detailed command references, visit the [Nexus CLI Git Repository](https://github.com/prism-coder/nexus-cli) page.
