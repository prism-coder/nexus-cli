import { Layer, Log, Event } from "@prism-dev/nexus";

/**
 * Your newly generated `<%= layerName %>Layer`.
 *
 * @export
 * @class <%= layerName %>Layer
 * @extends {Layer}
 */
export class <%= layerName %>Layer extends Layer {
    /**
     * Called when `<%= layerName %>Layer` is attached to the `LayerStack`.
     *
     * @memberof <%= layerName %>Layer
     */
    OnAttach(): void {
        Log.Info("<%= layerName %>Layer::OnAttach - Attaching <%= layerName %>Layer");
    }

    /**
     * Called when `<%= layerName %>Layer` is detached from the `LayerStack`.
     *
     * @memberof <%= layerName %>Layer
     */
    OnDetach(): void {
        Log.Info("<%= layerName %>Layer::OnDetach - Detaching <%= layerName %>Layer");
    }

    /**
     * Called every `Application` tick with a Timestep.
     *
     * @param {number} ts The time step since the last update.
     * @memberof <%= layerName %>Layer
     */
    OnUpdate(ts: number): void {
        // Log.Info("<%= layerName %>Layer::OnUpdate - Updating <%= layerName %>Layer");
    }

    /**
     * Called every time `<%= layerName %>Layer` receives an `Event`.
     *
     * @param {Event} event The `Event` received on `<%= layerName %>Layer`.
     * @memberof <%= layerName %>Layer
     */
    OnEvent(event: Event): void {
        Log.Info(`<%= layerName %>Layer::OnEvent - Received '${event.Name}' Event on <%= layerName %>Layer`);
    }
}