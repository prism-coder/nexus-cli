import { Event, EventCategory, EventType } from "@prism-dev/nexus";

/**
 * Interface that holds the payload of the `Event`.
 *
 * @export
 * @interface <%= eventName %>Payload
 */
export interface <%= eventName %>Payload {
    ID: number;
    Username: string;
    Name: string;
    Surname: string;
    Email: string;
    Phone: string;
    Birthday: Date;
    Image: string;
}

/**
 * Your newly generated `Event`.
 *
 * @export
 * @class <%= eventName %>
 * @extends {Event}
 */
export class <%= eventName %> extends Event {
    public readonly Name: string = "<%= eventName %>";
    public readonly Type: EventType = EventType.None;
    public readonly Category: EventCategory = EventCategory.None;

    public readonly Payload: <%= eventName %>Payload;

    constructor(payload: <%= eventName %>Payload) {
        super();
        this.Payload = payload;
    }
}